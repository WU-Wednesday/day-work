#include <stdio.h>
int Facl(int n)
{
    int i=0;
    int ret=1;
   if(n<=1)
      return 1;
   else
      return n*Facl(n-1)
}
int main()
{
    int n=0;
    int ret=0;
    printf("请输入要求的阶乘：");
    scanf("%d",&n);
    ret=Facl(n);
    printf("该数的阶乘为：%d\n",ret);
    return 0;
}
